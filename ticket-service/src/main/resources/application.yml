spring :
  application:
    name : ticket-service

  profiles :
    active : dev

  config:
    import: optional:configserver:http://localhost:9200

  cloud:
    config:
      discovery:
        enabled: false

service :
  api-gateway : api-gateway